//! Cargo package-name adapter for consumers that depend on the published
//! `gpui-pre` snapshot. All API and types come from the pinned Zed revision so
//! the application has a single GPUI implementation.

pub use gpui_current::*;
